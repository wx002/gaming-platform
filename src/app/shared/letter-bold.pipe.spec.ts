import { LetterBoldPipe } from './letter-bold.pipe';

describe('LetterBoldPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterBoldPipe();
    expect(pipe).toBeTruthy();
  });
});
